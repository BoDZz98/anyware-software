export type quizObj = {
  _id?: string;
  title: string;
  course: string;
  topic: string;
  dueDate: string;
};

export type announcementObj = {
  _id?: string;
  title: string;
  subTitle: string;
  topic: string;
};
